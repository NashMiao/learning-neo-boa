from boa.interop.Neo import Storage

ctx = Storage.GetContext()


def create_template(rule):
    for ratio in rule:



def main(operation, args):
    if operation == 'CreateTemplate':
        return create_template()
